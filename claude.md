你是一个资深前端架构和性能分析专家，任务是分析一个中型 Vue3 + Vite 项目的源码。请严格按照以下要求执行：

1. **读取范围**  
   - 只读取项目根目录下的：
     - `src/` 目录  
     - `tree_data真实数据示例.json`  
     - 根目录下的配置文件（如 package.json、vite.config.js、tsconfig.json、tailwind.config.js 等）  
   - **绝对禁止**读取 `node_modules` 或任何第三方依赖目录  
   - 忽略构建产物目录（如 dist、public/generated）  

2. **分析目标**  
   - **核心功能逻辑**  
     - 总结每个模块的功能  
     - 分析各组件、store、composables 的职责  
     - 描绘模块之间调用关系  
   - **性能分析（最重要）**  
     - 找出项目卡顿、响应慢、渲染阻塞的原因  
     - 重点关注：
       - 重渲染频率  
       - 状态管理响应机制  
       - 异步调用、API 请求、组件懒加载  
       - DOM 更新、虚拟列表、计算属性、watcher 等性能瓶颈  
       - 资源加载和打包优化问题  
   - **其它评估**  
     - 项目结构合理性  
     - 配置文件可优化点  
     - 依赖冗余或可替换方案  

3. **输出要求**  
   - 分析结果必须分门别类写入不同 Markdown 文件，保存在根目录下的 `doc/` 目录中：  
     - `doc/core_logic.md` → 核心功能逻辑分析  
     - `doc/performance.md` → 性能瓶颈分析（重点）  
     - `doc/other_evaluation.md` → 其它方面评估  
   - Markdown 文件内容必须清晰、有层次，可直接作为文档参考  
   - 输出格式示例：
     ```markdown
     # 性能分析
     ## 重渲染频率问题
     - xxx
     ## 状态管理响应问题
     - xxx
     ## 异步调用优化建议
     - xxx
     ```
   - 不要把分析结果混在一段长文本里，每类分析独立文件  

4. **操作规则**  
   - 分析时，优先使用项目真实文件内容  
   - 如果遇到 JSON 或配置文件，解析后提取有价值信息  
   - 输出中不要包含 node_modules 的任何信息  

请严格按照以上指令执行，并保证性能分析部分尽可能详细，列出每个可能导致卡顿或响应慢的因素。最终生成三个 Markdown 文件到 `doc/` 目录下。